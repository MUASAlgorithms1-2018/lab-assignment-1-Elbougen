#include <iostream>
#include "calc.h"
#include "tester.h"

using namespace std; //Übersichtlichkeitsgründe

int main() {
    tester();


    /* int arr[] = {-1, 1, -2, 2, -3, 3, -4};
    int n = sizeof(arr) / sizeof(int);
    int result = cubic_mss(arr, n);

    cout << result << endl;
    */

    return 0;
}